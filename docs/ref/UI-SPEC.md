# UI/UX 시안 스펙 — 풀다 AI

> design-to-code 하네스 STEP 2~3
> Stitch HTML에서 추출한 디자인 토큰 + shadcn 컴포넌트 매핑 + 레이아웃 스펙
>
> **상세 토큰:** `docs/design/DESIGN-TOKENS.md` 참조
> **반응형 기준:** `docs/ref/RESPONSIVE-GUIDE.md` 참조

---

## 디자인 토큰

> Stitch 시안 5개 HTML에서 추출. 라이트모드 + 모노크로매틱 웜 팔레트.

### 색상 시스템 (CSS Variables)

```css
@layer base {
  :root {
    --background: 40 20% 98%;        /* #faf9f7 — 따뜻한 흰색 */
    --foreground: 150 6% 10%;        /* #1a1c1b */
    --card: 0 0% 100%;               /* #ffffff */
    --card-foreground: 150 6% 10%;
    --popover: 0 0% 100%;
    --popover-foreground: 150 6% 10%;
    --primary: 0 0% 0%;              /* #000000 — 블랙 */
    --primary-foreground: 30 6% 89%; /* #e5e2e1 */
    --secondary: 0 1% 37%;           /* #5f5e5e */
    --secondary-foreground: 0 0% 100%;
    --muted: 30 6% 95%;              /* #f4f3f1 */
    --muted-foreground: 0 0% 28%;    /* #474747 */
    --accent: 30 5% 93%;             /* #efeeec */
    --accent-foreground: 150 6% 10%;
    --destructive: 0 73% 41%;        /* #ba1a1a */
    --destructive-foreground: 0 0% 100%;
    --border: 0 0% 78%;              /* #c6c6c6 */
    --input: 0 0% 47%;               /* #777777 */
    --ring: 0 0% 0%;
    --radius: 0.125rem;

    /* 의미 색상 (히트맵, 상태 표시) */
    --success: 142 76% 36%;
    --warning: 38 92% 50%;

    /* Grill-Me 질문 사다리 — 단계별 색상은 secondary 톤 유지 */
    --step-active: 0 0% 0%;          /* 현재 단계 */
    --step-done: 0 0% 47%;           /* 완료 단계 */
    --step-pending: 0 0% 78%;        /* 대기 단계 */
  }
}
```

### 표면 계층 (Stitch Material Design 3 기반)

```
surface-container-lowest:  #ffffff    ← 카드, 팝오버
surface-container-low:     #f4f3f1    ← 뮤트 배경
surface-container:         #efeeec    ← 섹션 배경
surface-container-high:    #eae8e5    ← 프로그레스 바 트랙
surface-container-highest: #e4e2df    ← 강조 배경
```

### 간격/크기

| 토큰 | 값 | 용도 |
|------|---|------|
| `page-padding` | `px-6 md:px-8 lg:px-12` | 페이지 좌우 여백 |
| `section-gap` | `space-y-16 md:space-y-24` | 섹션 간 (넉넉한 여백) |
| `card-padding` | `p-5 md:p-8` | 카드 내부 |
| `max-width` | `max-w-2xl` (폼/채팅), `max-w-4xl` (채팅), `max-w-7xl` (랜딩), `max-w-[1440px]` (대시보드) | 콘텐츠 너비 |
| `border-radius` | `--radius: 0.125rem` | 직각 미학 (에디토리얼) |

### 타이포그래피 스케일

| 용도 | 클래스 | 사이즈 |
|------|--------|--------|
| 히어로 제목 | `text-5xl font-extrabold tracking-tight` | 48px |
| 페이지 제목 | `text-[3.5rem] font-bold tracking-[-0.02em]` | 56px |
| 섹션 제목 | `text-2xl~3xl font-bold tracking-tight` | 24~30px |
| 카드 제목 | `text-xl font-bold` | 20px |
| 본문 | `text-base leading-relaxed` | 16px |
| 라벨 | `text-[0.75rem] font-bold uppercase tracking-[0.05em]` | 12px 대문자 |
| 마이크로 | `text-[10px] font-bold uppercase tracking-widest` | 10px |
| 수치 (stats) | `text-4xl~6xl font-extrabold tabular-nums` | 큰 숫자 |
| 수식 | KaTeX + `font-serif italic` | 수식 렌더링 |

---

## P-001: 랜딩 (`/`)

### 레이아웃

```
┌─────────────────────────────────────────────┐
│              (전체 화면, 중앙 정렬)            │
│                                             │
│         ┌─────────────────────┐             │
│         │    📐 로고/아이콘     │             │
│         │                     │             │
│         │   풀다 AI      │  text-4xl   │
│         │   수업 후 막힘 해소   │  text-lg    │
│         │   AI 코치            │  muted      │
│         └─────────────────────┘             │
│                                             │
│    ┌──────────────┐  ┌──────────────┐      │
│    │   👩‍🏫 교사로   │  │   👨‍🎓 학생으로  │      │
│    │   시작하기    │  │   시작하기    │      │
│    │              │  │              │      │
│    │  수업 자료    │  │  질문하고     │      │
│    │  등록하고     │  │  힌트 받기    │      │
│    │  학생 분석    │  │              │      │
│    └──────────────┘  └──────────────┘      │
│     Card + hover:     Card + hover:         │
│     border-primary    border-primary        │
│                                             │
│    "정답을 주지 않습니다.                     │
│     스스로 찾도록 질문합니다."                │
│     text-sm muted-foreground               │
│                                             │
│    ┌─ 하단 배지 ─────────────────────┐     │
│    │ 🔒 데이터 로컬 처리 가능          │     │
│    │ 🧠 Grill-Me 소크라틱 질문법       │     │
│    │ 📊 교사용 오개념 대시보드          │     │
│    └───────────────────────────────┘     │
└─────────────────────────────────────────────┘
```

### shadcn 컴포넌트

| 요소 | 컴포넌트 | props/클래스 |
|------|----------|-------------|
| 역할 카드 | `Card` + `CardHeader` + `CardContent` | `hover:border-primary cursor-pointer transition-colors` |
| 시작 버튼 | 카드 전체가 클릭 | `onClick → router.push()` |
| 하단 배지 | `Badge variant="secondary"` × 3 | `flex gap-2 justify-center` |
| 태그라인 | `<p>` | `text-sm text-muted-foreground text-center` |

### 인터랙션

1. 카드 hover → border-primary 강조
2. 클릭 → localStorage에 역할 저장 → 라우트 이동
3. 애니메이션: 카드 `hover:scale-[1.02] transition-transform`

---

## P-002: 교사 자료 등록 (`/teacher/upload`)

### 레이아웃

```
┌─────────────────────────────────────────────┐
│ ← 돌아가기                  교사 자료 등록    │  Header
├─────────────────────────────────────────────┤
│  max-w-2xl mx-auto                          │
│                                             │
│  ┌─ Card ─────────────────────────────┐    │
│  │ 수업 정보                           │    │
│  │                                    │    │
│  │ 수업 제목                           │    │
│  │ ┌────────────────────────────┐    │    │
│  │ │ 이차방정식의 풀이            │    │    │
│  │ └────────────────────────────┘    │    │
│  │                                    │    │
│  │ 핵심 주제                           │    │
│  │ ┌────────────────────────────┐    │    │
│  │ │ 인수분해, 근의 공식, 판별식  │    │    │
│  │ └────────────────────────────┘    │    │
│  └────────────────────────────────────┘    │
│                                             │
│  ┌─ Card ─────────────────────────────┐    │
│  │ 수업 자료 업로드                     │    │
│  │                                    │    │
│  │ ┌─ Dropzone ────────────────┐     │    │
│  │ │                           │     │    │
│  │ │   📄 PDF 또는 .md 파일     │     │    │
│  │ │   드래그하거나 클릭        │     │    │
│  │ │                           │     │    │
│  │ │   지원: PDF, Markdown     │     │    │
│  │ └───────────────────────────┘     │    │
│  │                                    │    │
│  │ 업로드된 자료:                      │    │
│  │ ┌────────────────────────────┐    │    │
│  │ │ 📄 이차방정식_개념.pdf      │    │    │
│  │ │ Badge: ✅ 처리 완료         │    │    │
│  │ └────────────────────────────┘    │    │
│  └────────────────────────────────────┘    │
│                                             │
│  ┌────────────────────────────────────┐    │
│  │     [ 지식베이스 생성하기 ]          │    │
│  │     Button variant="default" w-full │    │
│  └────────────────────────────────────┘    │
│                                             │
│  ┌─ Alert variant="success" ──────────┐    │
│  │ ✅ 지식베이스 생성 완료!             │    │
│  │ 학생이 이 수업에 대해 질문할 수       │    │
│  │ 있습니다.                           │    │
│  │                                    │    │
│  │ [ 대시보드 보기 ]  [ 링크 복사 ]     │    │
│  └────────────────────────────────────┘    │
└─────────────────────────────────────────────┘
```

### shadcn 컴포넌트

| 요소 | 컴포넌트 | 비고 |
|------|----------|------|
| 입력 필드 | `Input` + `Label` | `placeholder="이차방정식의 풀이"` |
| 드롭존 | `react-dropzone` + dashed border | `border-2 border-dashed rounded-xl p-12 text-center` |
| 파일 목록 | `Card` + `Badge` | 파일명 + 처리 상태 배지 |
| 처리 상태 | `Badge variant="success"` | ✅ 처리 완료 / ⏳ 처리중 / ❌ 실패 |
| 생성 버튼 | `Button size="lg"` | `w-full` |
| 완료 알림 | `Alert` | 성공 시 표시 |
| 진행 바 | `Progress` | 업로드/처리 중 표시 |

---

## P-003: 학생 질문 (`/student/ask`) — **핵심 페이지**

### 레이아웃

```
┌─────────────────────────────────────────────┐
│ ← 돌아가기    이차방정식    질문 2/4 ████░░  │  Header + Progress
├─────────────────────────────────────────────┤
│  max-w-2xl mx-auto                          │
│                                             │
│  ┌─ AI 메시지 (왼쪽 정렬) ──────────────┐  │
│  │ 🤖                                   │  │
│  │ 안녕! 이차방정식 수업에 대해            │  │
│  │ 궁금한 게 있으면 질문해줘.             │  │
│  │                                      │  │
│  │ 💡 Grill-Me 모드: 내가 질문을 던질게.  │  │
│  │ 스스로 생각하면서 답해봐!              │  │
│  └──────────────────────────────────────┘  │
│                                             │
│  ┌─ 학생 메시지 (오른쪽 정렬) ─────────┐   │
│  │                             👤      │   │
│  │    이 문제 어떻게 풀어요?           │   │
│  │    ┌──────────────┐               │   │
│  │    │ 📷 문제 사진  │               │   │
│  │    └──────────────┘               │   │
│  └──────────────────────────────────────┘  │
│                                             │
│  ┌─ AI 질문 메시지 ────────────────────┐   │
│  │ 🤖  질문 (1/4) 접근법              │   │
│  │                                    │   │
│  │ 이 이차방정식을 보면, 어떤 방법을    │   │
│  │ 사용하면 풀 수 있을 것 같아?        │   │
│  │                                    │   │
│  │ 📚 수업자료 근거 있음               │   │
│  │                                    │   │
│  │ ┌─ Collapsible ──────────────┐    │   │
│  │ │ ▶ 추천 보기                 │    │   │
│  │ │                            │    │   │
│  │ │ (열리면)                    │    │   │
│  │ │ 💡 추천: 인수분해를 사용하면 │    │   │
│  │ │ 이 식을 두 일차식의 곱으로  │    │   │
│  │ │ 분해할 수 있어.            │    │   │
│  │ └────────────────────────────┘    │   │
│  └──────────────────────────────────────┘  │
│                                             │
│  ┌─ 모드 전환 알림 (3회 오답 시) ──────┐   │
│  │ 🔄 Guide-Me 모드로 전환합니다.      │   │
│  │ 직접 설명해줄게.                    │   │
│  └──────────────────────────────────────┘  │
│                                             │
│ ┌───────────────────────────────────────┐  │
│ │ [  질문을 입력하세요...  ] [📷] [전송] │  │
│ │  Input + IconButton × 2               │  │
│ └───────────────────────────────────────┘  │
│                                             │
│ ┌─ 모드 선택 (하단 고정) ────────────────┐ │
│ │ [Grill-Me] [Guide-Me] [Quick-Me 🚨]  │ │
│ │ Tabs / ToggleGroup                    │ │
│ └───────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

### shadcn 컴포넌트

| 요소 | 컴포넌트 | 비고 |
|------|----------|------|
| 메시지 버블 (AI) | `Card` | `bg-card border ml-0 mr-12` |
| 메시지 버블 (학생) | `Card` | `bg-primary/10 border-primary/20 ml-12 mr-0` |
| 프로그레스 바 | `Progress` | 헤더에 질문 단계 표시 (1/4~4/4) |
| 단계 배지 | `Badge` | step 색상별 (indigo→blue→violet→green) |
| 추천 보기 | `Collapsible` + `CollapsibleTrigger` + `CollapsibleContent` | 접이식 |
| 근거 표시 | `Badge variant="secondary"` | "📚 수업자료 근거" |
| 입력창 | `Input` + `Button` (아이콘) | 하단 고정 `sticky bottom-0` |
| 이미지 업로드 | `Button variant="ghost"` + 📷 아이콘 | 파일 선택 트리거 |
| 모드 전환 | `ToggleGroup` | Grill-Me / Guide-Me / Quick-Me |
| 모드 알림 | `Alert` | 자동 전환 시 표시 |
| 마크다운 | `react-markdown` + `remark-math` + `rehype-katex` | AI 응답 렌더링 |
| 스크롤 | `ScrollArea` | 채팅 영역 |

### 메시지 버블 상세

```
AI 질문 메시지:
┌──────────────────────────────────────┐
│ 🤖  Badge("질문 1/4", step-1 색상)   │  ← 헤더
│                                      │
│ 이 이차방정식을 보면, 어떤 방법을      │  ← 마크다운 본문
│ 사용하면 풀 수 있을 것 같아?          │
│                                      │
│ $x^2 + 5x + 6 = 0$                  │  ← KaTeX 수식
│                                      │
│ Badge("📚 수업자료 근거")             │  ← 근거 플래그
│                                      │
│ ▶ 추천 보기  (Collapsible)           │  ← 접이식
│   💡 인수분해를 사용하면...            │
└──────────────────────────────────────┘
```

---

## P-004: 교사 대시보드 (`/teacher/dashboard`)

### 레이아웃

```
┌─────────────────────────────────────────────┐
│ ← 돌아가기                  교사 대시보드     │
├─────────────────────────────────────────────┤
│  max-w-4xl mx-auto                          │
│                                             │
│  이차방정식  |  2026-04-06                   │
│  학생 5명  |  질문 23개  |  회복률 64%        │
│                                             │
│  ┌─ 통계 카드 (3열 그리드) ──────────────┐  │
│  │ ┌────────┐ ┌────────┐ ┌────────┐    │  │
│  │ │  23    │ │  5     │ │  64%   │    │  │
│  │ │ 총 질문 │ │ 학생 수│ │ 회복률  │    │  │
│  │ └────────┘ └────────┘ └────────┘    │  │
│  └──────────────────────────────────────┘  │
│                                             │
│  ┌─ Card: 오개념 히트맵 ───────────────┐   │
│  │ 📊 오개념 히트맵                     │   │
│  │                                     │   │
│  │ 인수분해   ━━━━━━━━━━━━━━━━━━░░ 80% │   │
│  │            Progress bg-red-500       │   │
│  │                                     │   │
│  │ 판별식     ━━━━━━━━━━━░░░░░░░░ 50% │   │
│  │            Progress bg-amber-500     │   │
│  │                                     │   │
│  │ 근의 공식  ━━━━━━░░░░░░░░░░░░░ 30% │   │
│  │            Progress bg-blue-500      │   │
│  │                                     │   │
│  │ 완전제곱식 ━━━━░░░░░░░░░░░░░░░ 20% │   │
│  │            Progress bg-green-500     │   │
│  └──────────────────────────────────────┘  │
│                                             │
│  ┌───────────────┐ ┌───────────────────┐   │
│  │ Card: TOP 5   │ │ Card: 보충 추천   │   │
│  │               │ │                   │   │
│  │ 1. 인수분해   │ │ 💡 AI 추천        │   │
│  │    어떻게     │ │                   │   │
│  │    시작?      │ │ "인수분해에서     │   │
│  │              │ │  80%가 막힘.      │   │
│  │ 2. 근의 공식 │ │  합과 곱 관계를   │   │
│  │    언제?     │ │  시각적으로       │   │
│  │              │ │  복습 추천"       │   │
│  │ 3. 판별식?  │ │                   │   │
│  │ 4. x² ≠ 1  │ │ [보충 자료 생성]  │   │
│  │ 5. 허근?    │ │                   │   │
│  └───────────────┘ └───────────────────┘   │
│   (2열 그리드 md:grid-cols-2)               │
│                                             │
│  ┌─ Card: 최근 질문 로그 ──────────────┐   │
│  │ Table                               │   │
│  │ 학생 | 질문              | 의도 | 시각│   │
│  │ 홍길동| 인수분해 어떻게?  | hint | 3분│   │
│  │ 김철수| 근의 공식 언제?   |concept| 5분│   │
│  │ ...                                 │   │
│  └──────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

### shadcn 컴포넌트

| 요소 | 컴포넌트 | 비고 |
|------|----------|------|
| 통계 카드 | `Card` × 3 | `grid grid-cols-3 gap-4` |
| 히트맵 바 | `Progress` | 빈도별 색상 (80%→red, 50%→amber, 30%→blue, 20%→green) |
| TOP 5 | `Card` + 번호 리스트 | 질문 원문 표시 |
| 보충 추천 | `Card` | AI 생성 텍스트 + 액션 버튼 |
| 질문 로그 | `Table` + `TableHeader` + `TableRow` | 의도별 Badge 색상 |
| 의도 배지 | `Badge` | concept=blue, hint=indigo, review=violet, similar=green |

### 히트맵 색상 규칙

모노크로매틱 디자인 원칙에 따라 **단일 primary 색상** 사용. 바 길이(%)로 심각도 표현.

| 요소 | 색상 | 비고 |
|------|------|------|
| 프로그레스 바 | `bg-primary` (#000000) | 단일 색상 |
| 트랙 | `bg-surface-container-high` (#eae8e5) | 배경 |
| 퍼센트 텍스트 | `font-mono text-muted-foreground` | 우측 정렬 |

---

## P-005: 결과/요약 (`/student/summary`)

### 레이아웃

```
┌─────────────────────────────────────────────┐
│ ← 돌아가기                    학습 요약       │
├─────────────────────────────────────────────┤
│  max-w-lg mx-auto text-center               │
│                                             │
│       오늘의 학습 요약                       │
│       이차방정식 | 2026-04-06               │
│                                             │
│  ┌─ 통계 카드 (3열) ───────────────────┐   │
│  │ ┌────────┐ ┌────────┐ ┌────────┐   │   │
│  │ │  ✅ 2  │ │  📝 5  │ │  💡 8  │   │   │
│  │ │ 해결한 │ │  질문  │ │  힌트  │   │   │
│  │ │  막힘  │ │  횟수  │ │  받음  │   │   │
│  │ └────────┘ └────────┘ └────────┘   │   │
│  └──────────────────────────────────────┘  │
│                                             │
│  ┌─ Card: 다룬 개념 ──────────────────┐    │
│  │                                     │    │
│  │ ✅ 인수분해 기초     → 이해 완료     │    │
│  │    Badge: success                   │    │
│  │    Separator                        │    │
│  │ 🔄 근의 공식         → 추가 연습     │    │
│  │    Badge: warning                   │    │
│  │                                     │    │
│  └──────────────────────────────────────┘  │
│                                             │
│  ┌─ 버튼 그룹 ────────────────────────┐    │
│  │ [ 다시 질문하기 ]  [ 처음으로 ]      │    │
│  │  Button default     Button outline  │    │
│  └──────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

### shadcn 컴포넌트

| 요소 | 컴포넌트 | 비고 |
|------|----------|------|
| 통계 | `Card` × 3 | `grid grid-cols-3 gap-4` |
| 개념 목록 | `Card` + `Separator` | 이해/미이해 아이콘 |
| 상태 배지 | `Badge variant="success"` / `"warning"` | ✅ 이해 / 🔄 연습 |
| 다시 질문 | `Button` | `variant="default"` |
| 처음으로 | `Button` | `variant="outline"` |

---

## 공통 레이아웃

### Header 컴포넌트

```
┌─────────────────────────────────────────────┐
│ [← 돌아가기]  풀다 AI  [교사 👩‍🏫 | 학생 👨‍🎓] │
│  Button ghost   text-sm      Badge role     │
└─────────────────────────────────────────────┘
```

- 랜딩에서는 Header 숨김
- 나머지 페이지에서 상단 고정 `sticky top-0 z-50 bg-background/80 backdrop-blur`

### 반응형

| 뷰포트 | 너비 | 레이아웃 변화 |
|--------|------|-------------|
| 모바일 | `< 768px` | 1열, 풀너비 |
| 태블릿+ | `≥ 768px` | 대시보드 2열 그리드 |
| 데스크톱 | `≥ 1024px` | max-width 적용 |

### 로딩/에러/빈 상태

| 상태 | UI | 컴포넌트 |
|------|---|----------|
| 로딩 | `Skeleton` 3줄 | shadcn `Skeleton` |
| 에러 | "다시 시도" 버튼 | `Alert variant="destructive"` + `Button` |
| 빈 상태 | 안내 메시지 + CTA | `Card` + muted 텍스트 |
| AI 스트리밍 | 깜빡이는 커서 | `animate-pulse` + `▊` |
